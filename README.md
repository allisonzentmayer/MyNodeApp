This app interacts with The Movie Database (https://www.themoviedb.org/) and returns movie data

To run the app:

`export PORT=8080`

`npm install`

`node app.js` OR `nodemon app.js` for live reload during development


Coming soon in next release:
- Caching
